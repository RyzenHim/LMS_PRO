import React from 'react'
import { Outlet } from 'react-router-dom'

const AuthMainPage = () => {
    return (
        <div>

            <Outlet />
        </div>
    )
}

export default AuthMainPage